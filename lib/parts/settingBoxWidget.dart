import 'package:flutter/material.dart';

class SettingBoxWidget extends StatelessWidget {
  const SettingBoxWidget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context)
  => const SizedBox(
      child: Center(
        child: Text('button'),
      )
  );
}