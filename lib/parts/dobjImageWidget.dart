import 'package:flutter/material.dart';

class DObjImageWidget extends StatelessWidget {
  const DObjImageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text('Caption Image Widget'),
    );
  }
}
