import 'package:flutter/material.dart';

class DObjTextWidget extends StatelessWidget {
  const DObjTextWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text('Caption Image Widget'),
    );
  }
}
